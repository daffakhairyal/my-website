import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Almayrizqd | My Portfolio",
  description: "Welcome to my portfolio! I'm Daffa Khairy Almayrizq, a web developer and Excel expert passionate about creating efficient digital solutions. With a strong background in web development and data automation, I build interactive websites and optimize workflows using Excel. Explore my projects to see how I combine technology and functionality to solve real-world problems. Feel free to reach out for collaboration!",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
